---
title: Embedders
description: 29 embedding providers including OpenAI, Cohere, HuggingFace, and local options.
---

Embedders convert text into vectors for similarity search. Agno supports 29 embedding providers with a consistent interface.

```python
from agno.knowledge.embedder.openai import OpenAIEmbedder
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.pgvector import PgVector

knowledge = Knowledge(
    vector_db=PgVector(
        table_name="docs",
        db_url="postgresql://...",
        embedder=OpenAIEmbedder(id="text-embedding-3-small"),
    ),
)
```

## Supported Embedders

| Provider | Import | Models |
|----------|--------|--------|
| OpenAI | `from agno.knowledge.embedder.openai import OpenAIEmbedder` | text-embedding-3-small, text-embedding-3-large |
| Azure OpenAI | `from agno.knowledge.embedder.azure_openai import AzureOpenAIEmbedder` | text-embedding-3-small via Azure |
| Cohere | `from agno.knowledge.embedder.cohere import CohereEmbedder` | embed-english-v3.0, embed-multilingual-v3.0 |
| Google | `from agno.knowledge.embedder.google import GeminiEmbedder` | gemini-embedding-001 |
| Mistral | `from agno.knowledge.embedder.mistral import MistralEmbedder` | mistral-embed |
| HuggingFace | `from agno.knowledge.embedder.huggingface import HuggingfaceCustomEmbedder` | Any sentence-transformer |
| Ollama | `from agno.knowledge.embedder.ollama import OllamaEmbedder` | nomic-embed-text, mxbai-embed-large |
| Jina | `from agno.knowledge.embedder.jina import JinaEmbedder` | jina-embeddings-v3 |
| Fireworks | `from agno.knowledge.embedder.fireworks import FireworksEmbedder` | nomic-embed-text-v1.5 |
| AWS Bedrock | `from agno.knowledge.embedder.aws_bedrock import AwsBedrockEmbedder` | Cohere Embed v3/v4 via Bedrock |
| Voyage | `from agno.knowledge.embedder.voyageai import VoyageAIEmbedder` | voyage-2, voyage-code-2 |

## Examples by Provider

### OpenAI

The most common choice for production systems.

```python cookbook/07_knowledge/embedders/openai_embedder.py
from agno.knowledge.embedder.openai import OpenAIEmbedder
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.pgvector import PgVector

embedder = OpenAIEmbedder(id="text-embedding-3-small")

# Get embeddings directly
embeddings = embedder.get_embedding("Hello, world!")
print(f"Dimensions: {len(embeddings)}")  # 1536

# Use in knowledge base
knowledge = Knowledge(
    vector_db=PgVector(
        table_name="docs",
        db_url="postgresql://...",
        embedder=embedder,
    ),
)
```

### OpenAI with Batching

Efficient embedding for large document sets.

```python cookbook/07_knowledge/embedders/openai_embedder_batching.py
from agno.knowledge.embedder.openai import OpenAIEmbedder

embedder = OpenAIEmbedder(
    id="text-embedding-3-small",
    enable_batch=True,
    batch_size=100,  # Process 100 texts at a time
)
```

### Cohere

Strong multilingual support.

```python cookbook/07_knowledge/embedders/cohere_embedder.py
from agno.knowledge.embedder.cohere import CohereEmbedder

embedder = CohereEmbedder(
    id="embed-english-v3.0",
    input_type="search_document",
)
```

### Google Gemini

Google's embedding models.

```python cookbook/07_knowledge/embedders/gemini_embedder.py
from agno.knowledge.embedder.google import GeminiEmbedder

embedder = GeminiEmbedder(id="gemini-embedding-001")
```

### Azure OpenAI

OpenAI embeddings through Azure.

```python cookbook/07_knowledge/embedders/azure_embedder.py
from agno.knowledge.embedder.azure_openai import AzureOpenAIEmbedder

embedder = AzureOpenAIEmbedder(
    id="text-embedding-3-small",
    azure_endpoint="https://your-resource.openai.azure.com/",
    api_version="2024-02-01",
)
```

### HuggingFace

Use any sentence-transformer model locally.

```python cookbook/07_knowledge/embedders/huggingface_embedder.py
from agno.knowledge.embedder.huggingface import HuggingfaceCustomEmbedder

embedder = HuggingfaceCustomEmbedder(
    id="sentence-transformers/all-MiniLM-L6-v2",
)
```

### Ollama

Local embeddings with Ollama.

```python cookbook/07_knowledge/embedders/ollama_embedder.py
from agno.knowledge.embedder.ollama import OllamaEmbedder

embedder = OllamaEmbedder(
    id="nomic-embed-text",
    dimensions=768,
    host="http://localhost:11434",
)
```

### Mistral

Mistral's embedding model.

```python cookbook/07_knowledge/embedders/mistral_embedder.py
from agno.knowledge.embedder.mistral import MistralEmbedder

embedder = MistralEmbedder(id="mistral-embed")
```

### Jina

Specialized embeddings for various tasks.

```python cookbook/07_knowledge/embedders/jina_embedder.py
from agno.knowledge.embedder.jina import JinaEmbedder

embedder = JinaEmbedder(id="jina-embeddings-v3")
```

### AWS Bedrock

Embeddings through AWS Bedrock.

```python cookbook/07_knowledge/embedders/aws_bedrock_embedder.py
from agno.knowledge.embedder.aws_bedrock import AwsBedrockEmbedder

embedder = AwsBedrockEmbedder(
    id="cohere.embed-multilingual-v3",
    aws_region="us-east-1",
)
```

### Fireworks

Fast inference for open-source embedding models.

```python cookbook/07_knowledge/embedders/fireworks_embedder.py
from agno.knowledge.embedder.fireworks import FireworksEmbedder

embedder = FireworksEmbedder(
    id="nomic-ai/nomic-embed-text-v1.5",
)
```

## Run Examples

```bash
git clone https://github.com/agno-agi/agno.git
cd agno/cookbook/07_knowledge/embedders

export OPENAI_API_KEY=xxx

# OpenAI
python openai_embedder.py

# HuggingFace (local)
python huggingface_embedder.py

# Ollama (local)
python ollama_embedder.py
```


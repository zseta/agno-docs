---
title: "Llama OpenAI Tool Use"
description: "Call YFinance tools from Llama 4 Maverick over the OpenAI-compatible API in sync and async modes."
source: cookbook/90_models/meta/llama_openai/tool_use.py
---

```python tool_use.py
"""Run `uv pip install openai yfinance` to install dependencies."""

import asyncio

from agno.agent import Agent
from agno.models.meta import LlamaOpenAI
from agno.tools.yfinance import YFinanceTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=LlamaOpenAI(id="Llama-4-Maverick-17B-128E-Instruct-FP8"),
    tools=[YFinanceTools()],
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("Whats the price of AAPL stock?")

    # --- Sync + Streaming ---
    agent.print_response("Whats the price of AAPL stock?", stream=True)

    # --- Async ---
    asyncio.run(agent.aprint_response("Whats the price of AAPL stock?"))

    # --- Async + Streaming ---
    asyncio.run(agent.aprint_response("Whats the price of AAPL stock?", stream=True))
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno llama-api-client yfinance
    ```
  </Step>

  <Step title="Export your Meta Llama API key">
    <CodeGroup>
    ```bash Mac/Linux
    export LLAMA_API_KEY="your_llama_api_key_here"
    ```

    ```bash Windows
    $Env:LLAMA_API_KEY="your_llama_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `tool_use.py`, then run:
    ```bash
    python tool_use.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/meta/llama_openai/tool_use.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/meta/llama_openai/tool_use.py)

---
title: "Cohere Image Agent Local File"
description: "Analyze a local image file with Cohere's Aya Vision model using Image(filepath=...)."
source: cookbook/90_models/cohere/image_agent_local_file.py
---

```python image_agent_local_file.py
"""
Cohere Image Agent Local File
=============================

Cookbook example for `cohere/image_agent_local_file.py`.
"""

from pathlib import Path

from agno.agent import Agent
from agno.media import Image
from agno.models.cohere.chat import Cohere
from agno.utils.media import download_image

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=Cohere(id="c4ai-aya-vision-8b"),
    markdown=True,
)

image_path = Path(__file__).parent.joinpath("sample.jpg")

download_image(
    url="https://upload.wikimedia.org/wikipedia/commons/0/0c/GoldenGateBridge-001.jpg",
    output_path=str(image_path),
)

agent.print_response(
    "Tell me about this image.",
    images=[
        Image(filepath=image_path),
    ],
    stream=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno cohere
    ```
  </Step>

  <Step title="Export your Cohere API key">
    <CodeGroup>
    ```bash Mac/Linux
    export CO_API_KEY="your_co_api_key_here"
    ```

    ```bash Windows
    $Env:CO_API_KEY="your_co_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `image_agent_local_file.py`, then run:
    ```bash
    python image_agent_local_file.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/cohere/image_agent_local_file.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/cohere/image_agent_local_file.py)

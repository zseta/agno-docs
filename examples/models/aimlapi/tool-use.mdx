---
title: "Tool Use"
description: "Answer current-events questions with web search tools on an AIML API model, sync and async."
source: cookbook/90_models/aimlapi/tool_use.py
---

```python tool_use.py
"""Run `pip install ddgs` to install dependencies."""

import asyncio

from agno.agent import Agent
from agno.models.aimlapi import AIMLAPI
from agno.tools.websearch import WebSearchTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=AIMLAPI(id="gpt-4o-mini"),
    tools=[WebSearchTools()],
    markdown=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("Whats happening in France?")

    # --- Async + Streaming ---
    asyncio.run(agent.aprint_response("Whats happening in France?", stream=True))
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs openai
    ```
  </Step>

  <Step title="Export your AI/ML API API key">
    <CodeGroup>
    ```bash Mac/Linux
    export AIMLAPI_API_KEY="your_aimlapi_api_key_here"
    ```

    ```bash Windows
    $Env:AIMLAPI_API_KEY="your_aimlapi_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `tool_use.py`, then run:
    ```bash
    python tool_use.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/aimlapi/tool_use.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/aimlapi/tool_use.py)

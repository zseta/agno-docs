---
title: "Aimlapi Image Agent Bytes"
description: "Pass an image as raw bytes to a Llama vision model served through AIMLAPI."
source: cookbook/90_models/aimlapi/image_agent_bytes.py
---

```python image_agent_bytes.py
"""
Aimlapi Image Agent Bytes
=========================

Cookbook example for `aimlapi/image_agent_bytes.py`.
"""

from pathlib import Path

from agno.agent import Agent
from agno.media import Image
from agno.models.aimlapi import AIMLAPI

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=AIMLAPI(id="meta-llama/Llama-3.2-11B-Vision-Instruct-Turbo"),
    markdown=True,
)

image_path = Path(__file__).parent.joinpath("sample.jpg")

# Read the image file content as bytes
image_bytes = image_path.read_bytes()

agent.print_response(
    "Tell me about this image",
    images=[
        Image(content=image_bytes),
    ],
    stream=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno openai
    ```
  </Step>

  <Step title="Export your AI/ML API API key">
    <CodeGroup>
    ```bash Mac/Linux
    export AIMLAPI_API_KEY="your_aimlapi_api_key_here"
    ```

    ```bash Windows
    $Env:AIMLAPI_API_KEY="your_aimlapi_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `image_agent_bytes.py`, then run:
    ```bash
    python image_agent_bytes.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/aimlapi/image_agent_bytes.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/aimlapi/image_agent_bytes.py)

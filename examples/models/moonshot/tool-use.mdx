---
title: "Moonshot Tool Use"
description: "Add WebSearchTools to a Moonshot Kimi K2 agent and stream a current-events answer."
source: cookbook/90_models/moonshot/tool_use.py
---

```python tool_use.py
"""
Moonshot Tool Use
=================

Cookbook example for `moonshot/tool_use.py`.
"""

from agno.agent import Agent
from agno.models.moonshot import MoonShot
from agno.tools.websearch import WebSearchTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=MoonShot(id="kimi-k2-thinking"),
    markdown=True,
    tools=[WebSearchTools()],
)

agent.print_response("What is happening in France?", stream=True)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs openai
    ```
  </Step>

  <Step title="Export your Moonshot API key">
    <CodeGroup>
    ```bash Mac/Linux
    export MOONSHOT_API_KEY="your_moonshot_api_key_here"
    ```

    ```bash Windows
    $Env:MOONSHOT_API_KEY="your_moonshot_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `tool_use.py`, then run:
    ```bash
    python tool_use.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/moonshot/tool_use.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/moonshot/tool_use.py)

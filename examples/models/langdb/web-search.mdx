---
title: "Web Search"
description: "Answer current-events questions with web search tools through LangDB."
source: cookbook/90_models/langdb/web_search.py
---

```python web_search.py
"""Run `uv pip install ddgs` to install dependencies."""

from agno.agent import Agent
from agno.models.langdb import LangDB
from agno.tools.websearch import WebSearchTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=LangDB(id="llama3-1-70b-instruct-v1.0"),
    tools=[WebSearchTools()],
    markdown=True,
)
agent.print_response("Whats happening in France?", stream=True)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs openai
    ```
  </Step>

  <Step title="Export your LangDB API key">
    <CodeGroup>
    ```bash Mac/Linux
    export LANGDB_API_KEY="your_langdb_api_key_here"
    ```

    ```bash Windows
    $Env:LANGDB_API_KEY="your_langdb_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `web_search.py`, then run:
    ```bash
    python web_search.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/langdb/web_search.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/langdb/web_search.py)

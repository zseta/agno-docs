---
title: "AWS Image Agent Bytes"
description: "Send image bytes to Amazon Nova Pro on Bedrock and fetch related news with web search."
source: cookbook/90_models/aws/bedrock/image_agent_bytes.py
---

```python image_agent_bytes.py
"""
Aws Image Agent Bytes
=====================

Cookbook example for `aws/bedrock/image_agent_bytes.py`.
"""

from pathlib import Path

from agno.agent import Agent
from agno.media import Image
from agno.models.aws import AwsBedrock
from agno.tools.websearch import WebSearchTools
from agno.utils.media import download_image

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=AwsBedrock(id="amazon.nova-pro-v1:0"),
    tools=[WebSearchTools()],
    markdown=True,
)

image_path = Path(__file__).parent.joinpath("sample.jpg")

download_image(
    url="https://upload.wikimedia.org/wikipedia/commons/0/0c/GoldenGateBridge-001.jpg",
    output_path=str(image_path),
)

# Read the image file content as bytes
image_bytes = image_path.read_bytes()

agent.print_response(
    "Tell me about this image and give me the latest news about it.",
    images=[
        Image(content=image_bytes, format="jpeg"),
    ],
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno aioboto3 boto3 ddgs
    ```
  </Step>

  <Step title="Export your API keys">
    <CodeGroup>
    ```bash Mac/Linux
    export AWS_ACCESS_KEY_ID="your_aws_access_key_id_here"
    export AWS_SECRET_ACCESS_KEY="your_aws_secret_access_key_here"
    ```

    ```bash Windows
    $Env:AWS_ACCESS_KEY_ID="your_aws_access_key_id_here"
    $Env:AWS_SECRET_ACCESS_KEY="your_aws_secret_access_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `image_agent_bytes.py`, then run:
    ```bash
    python image_agent_bytes.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/aws/bedrock/image_agent_bytes.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/aws/bedrock/image_agent_bytes.py)

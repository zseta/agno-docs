---
title: "Together Image Agent"
description: "Describe an image from a URL with Llama Vision on Together, streaming the response."
source: cookbook/90_models/together/image_agent.py
---

```python image_agent.py
"""
Together Image Agent
====================

Cookbook example for `together/image_agent.py`.
"""

from agno.agent import Agent
from agno.media import Image
from agno.models.together import Together

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=Together(id="meta-llama/Llama-Vision-Free"),
    markdown=True,
)

agent.print_response(
    "Tell me about this image",
    images=[
        Image(
            url="https://upload.wikimedia.org/wikipedia/commons/0/0c/GoldenGateBridge-001.jpg"
        )
    ],
    stream=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno openai
    ```
  </Step>

  <Step title="Export your Together API key">
    <CodeGroup>
    ```bash Mac/Linux
    export TOGETHER_API_KEY="your_together_api_key_here"
    ```

    ```bash Windows
    $Env:TOGETHER_API_KEY="your_together_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `image_agent.py`, then run:
    ```bash
    python image_agent.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/together/image_agent.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/together/image_agent.py)

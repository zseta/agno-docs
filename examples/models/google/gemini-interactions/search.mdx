---
title: "Gemini Interactions - Google Search"
description: "Example using the built-in Google Search tool with the Interactions API."
source: cookbook/90_models/google/gemini_interactions/search.py
---

```python search.py
"""
Gemini Interactions - Google Search
====================================

Example using the built-in Google Search tool with the Interactions API.
"""

import asyncio

from agno.agent import Agent
from agno.models.google import GeminiInteractions

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=GeminiInteractions(
        id="gemini-3.5-flash",
        search=True,
    ),
    markdown=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("What are the latest developments in quantum computing?")

    # --- Streaming ---
    asyncio.run(
        agent.aprint_response("What are the top news stories today?", stream=True)
    )
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno google-genai
    ```
  </Step>

  <Step title="Export your Google API key">
    <CodeGroup>
    ```bash Mac/Linux
    export GOOGLE_API_KEY="your_google_api_key_here"
    ```

    ```bash Windows
    $Env:GOOGLE_API_KEY="your_google_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `search.py`, then run:
    ```bash
    python search.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/google/gemini_interactions/search.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/google/gemini_interactions/search.py)

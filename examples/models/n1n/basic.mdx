---
title: "N1N Basic"
description: "Run GPT-4o through the N1N gateway, with and without streaming."
source: cookbook/90_models/n1n/basic.py
---

```python basic.py
"""
N1N Basic
=========

Cookbook example for `n1n/basic.py`.
"""

from agno.agent import Agent
from agno.models.n1n import N1N

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(model=N1N(id="gpt-4o"), markdown=True)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("Share a 2 sentence horror story.")

    # --- Sync + Streaming ---
    agent.print_response("Share a 2 sentence horror story.", stream=True)
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno openai
    ```
  </Step>

  <Step title="Export your N1N API key">
    <CodeGroup>
    ```bash Mac/Linux
    export N1N_API_KEY="your_n1n_api_key_here"
    ```

    ```bash Windows
    $Env:N1N_API_KEY="your_n1n_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `basic.py`, then run:
    ```bash
    python basic.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/n1n/basic.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/n1n/basic.py)

---
title: "Tool Use"
description: "Stream a web-search agent through the Requesty gateway, sync and async."
source: cookbook/90_models/requesty/tool_use.py
---

```python tool_use.py
"""Run `uv pip install ddgs` to install dependencies."""

import asyncio

from agno.agent import Agent
from agno.models.requesty import Requesty
from agno.tools.websearch import WebSearchTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=Requesty(id="openai/gpt-4o"),
    tools=[WebSearchTools()],
    markdown=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync + Streaming ---
    agent.print_response("Whats happening in France?", stream=True)

    # --- Async + Streaming ---
    asyncio.run(agent.aprint_response("Whats happening in France?", stream=True))
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs openai
    ```
  </Step>

  <Step title="Export your Requesty API key">
    <CodeGroup>
    ```bash Mac/Linux
    export REQUESTY_API_KEY="your_requesty_api_key_here"
    ```

    ```bash Windows
    $Env:REQUESTY_API_KEY="your_requesty_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `tool_use.py`, then run:
    ```bash
    python tool_use.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/requesty/tool_use.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/requesty/tool_use.py)

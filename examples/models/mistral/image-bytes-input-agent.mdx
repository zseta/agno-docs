---
title: "Mistral Image Bytes Input Agent"
description: "Pass raw image bytes fetched with requests to a Pixtral agent for analysis."
source: cookbook/90_models/mistral/image_bytes_input_agent.py
---

```python image_bytes_input_agent.py
"""
Mistral Image Bytes Input Agent
===============================

Cookbook example for `mistral/image_bytes_input_agent.py`.
"""

import requests
from agno.agent import Agent
from agno.media import Image
from agno.models.mistral.mistral import MistralChat

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=MistralChat(id="pixtral-12b-2409"),
    markdown=True,
)

image_url = (
    "https://tripfixers.com/wp-content/uploads/2019/11/eiffel-tower-with-snow.jpeg"
)


def fetch_image_bytes(url: str) -> bytes:
    headers = {
        "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36",
        "Accept": "image/avif,image/webp,image/apng,image/svg+xml,image/*,*/*;q=0.8",
        "Accept-Language": "en-US,en;q=0.9",
    }
    response = requests.get(url, headers=headers)
    response.raise_for_status()
    return response.content


image_bytes_from_url = fetch_image_bytes(image_url)

agent.print_response(
    "Tell me about this image.",
    images=[
        Image(content=image_bytes_from_url),
    ],
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno mistralai requests
    ```
  </Step>

  <Step title="Export your Mistral API key">
    <CodeGroup>
    ```bash Mac/Linux
    export MISTRAL_API_KEY="your_mistral_api_key_here"
    ```

    ```bash Windows
    $Env:MISTRAL_API_KEY="your_mistral_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `image_bytes_input_agent.py`, then run:
    ```bash
    python image_bytes_input_agent.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/mistral/image_bytes_input_agent.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/mistral/image_bytes_input_agent.py)

---
title: "Ollama Image Agent"
description: "Describe a local image file with Llama 3.2 Vision running on Ollama."
source: cookbook/90_models/ollama/chat/image_agent.py
---

```python image_agent.py
"""
Ollama Image Agent
==================

Cookbook example for `ollama/chat/image_agent.py`.
"""

from pathlib import Path

from agno.agent import Agent
from agno.media import Image
from agno.models.ollama import Ollama

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=Ollama(id="llama3.2-vision"),
    markdown=True,
)

image_path = Path(__file__).parent.joinpath("super-agents.png")
agent.print_response(
    "Write a 3 sentence fiction story about the image",
    images=[Image(filepath=image_path)],
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ollama
    ```
  </Step>

  <Step title="Run the example">
    Save the code above as `image_agent.py`, then run:
    ```bash
    python image_agent.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/ollama/chat/image_agent.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/ollama/chat/image_agent.py)

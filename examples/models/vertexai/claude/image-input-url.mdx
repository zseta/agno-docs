---
title: "Vertex AI Image Input URL"
description: "Pass an image URL to Claude on Vertex AI and search the web for more context."
source: cookbook/90_models/vertexai/claude/image_input_url.py
---

```python image_input_url.py
"""
Vertexai Image Input Url
========================

Cookbook example for `vertexai/claude/image_input_url.py`.
"""

from agno.agent import Agent
from agno.media import Image
from agno.models.vertexai.claude import Claude
from agno.tools.websearch import WebSearchTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=Claude(id="claude-sonnet-4@20250514"),
    tools=[WebSearchTools()],
    markdown=True,
)

agent.print_response(
    "Tell me about this image and search the web for more information.",
    images=[
        Image(
            url="https://fastly.picsum.photos/id/237/200/300.jpg?hmac=TmmQSbShHz9CdQm0NkEjx1Dyh_Y984R9LpNrpvH2D_U"
        ),
    ],
    stream=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs google-genai
    ```
  </Step>

  <Step title="Run the example">
    Save the code above as `image_input_url.py`, then run:
    ```bash
    python image_input_url.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/vertexai/claude/image_input_url.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/vertexai/claude/image_input_url.py)

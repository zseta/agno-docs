---
title: "Vertex AI PDF Input Bytes"
description: "Summarize a PDF passed as raw bytes to Claude Sonnet 4 on Vertex AI."
source: cookbook/90_models/vertexai/claude/pdf_input_bytes.py
---

```python pdf_input_bytes.py
"""
Vertexai Pdf Input Bytes
========================

Cookbook example for `vertexai/claude/pdf_input_bytes.py`.
"""

from pathlib import Path

from agno.agent import Agent
from agno.media import File
from agno.models.vertexai.claude import Claude
from agno.utils.media import download_file

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

pdf_path = Path(__file__).parent.joinpath("ThaiRecipes.pdf")

# Download the file using the download_file function
download_file(
    "https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf", str(pdf_path)
)

agent = Agent(
    model=Claude(id="claude-sonnet-4@20250514"),
    markdown=True,
)

agent.print_response(
    "Summarize the contents of the attached file.",
    files=[
        File(
            content=pdf_path.read_bytes(),
        ),
    ],
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno google-genai
    ```
  </Step>

  <Step title="Run the example">
    Save the code above as `pdf_input_bytes.py`, then run:
    ```bash
    python pdf_input_bytes.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/vertexai/claude/pdf_input_bytes.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/vertexai/claude/pdf_input_bytes.py)

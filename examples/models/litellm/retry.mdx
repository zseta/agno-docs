---
title: "Retry"
description: "Example demonstrating how to set up retries with LiteLLM."
source: cookbook/90_models/litellm/retry.py
---

```python retry.py
"""Example demonstrating how to set up retries with LiteLLM."""

from agno.agent import Agent
from agno.models.litellm import LiteLLM

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

# We will use a deliberately wrong model ID, to trigger retries.
wrong_model_id = "litellm-wrong-id"

agent = Agent(
    model=LiteLLM(
        id=wrong_model_id,
        retries=3,  # Number of times to retry the request.
        delay_between_retries=1,  # Delay between retries in seconds.
        exponential_backoff=True,  # If True, the delay between retries is doubled each time.
    ),
)

agent.print_response("What is the capital of France?")

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno litellm
    ```
  </Step>

  <Step title="Export your LiteLLM API key">
    <CodeGroup>
    ```bash Mac/Linux
    export LITELLM_API_KEY="your_litellm_api_key_here"
    ```

    ```bash Windows
    $Env:LITELLM_API_KEY="your_litellm_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `retry.py`, then run:
    ```bash
    python retry.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/litellm/retry.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/litellm/retry.py)

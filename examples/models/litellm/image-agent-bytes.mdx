---
title: "LiteLLM Image Agent Bytes"
description: "Send an image as raw bytes to gpt-4o via LiteLLM and fetch related news with web search."
source: cookbook/90_models/litellm/image_agent_bytes.py
---

```python image_agent_bytes.py
"""
Litellm Image Agent Bytes
=========================

Cookbook example for `litellm/image_agent_bytes.py`.
"""

from pathlib import Path

from agno.agent import Agent
from agno.media import Image
from agno.models.litellm import LiteLLM
from agno.tools.websearch import WebSearchTools
from agno.utils.media import download_image

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=LiteLLM(id="gpt-4o"),
    tools=[WebSearchTools()],
    markdown=True,
)

image_path = Path(__file__).parent.joinpath("sample.jpg")

download_image(
    url="https://upload.wikimedia.org/wikipedia/commons/0/0c/GoldenGateBridge-001.jpg",
    output_path=str(image_path),
)

# Read the image file content as bytes
image_bytes = image_path.read_bytes()

agent.print_response(
    "Tell me about this image and give me the latest news about it.",
    images=[
        Image(content=image_bytes),
    ],
    stream=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs litellm
    ```
  </Step>

  <Step title="Export your LiteLLM API key">
    <CodeGroup>
    ```bash Mac/Linux
    export LITELLM_API_KEY="your_litellm_api_key_here"
    ```

    ```bash Windows
    $Env:LITELLM_API_KEY="your_litellm_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `image_agent_bytes.py`, then run:
    ```bash
    python image_agent_bytes.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/litellm/image_agent_bytes.py](https://github.com/agno-agi/agno/blob/main/cookbook/90_models/litellm/image_agent_bytes.py)

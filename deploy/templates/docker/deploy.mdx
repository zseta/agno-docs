---
title: "AgentOS on self-hosted Docker"
sidebarTitle: "Deploy"
description: "Set up the self-hosted Starter with a coding-agent prompt, run it on Docker, and take the same Compose stack to production on any host you control."
---

[`agno-agi/agentos-docker`](https://github.com/agno-agi/agentos-docker) is the self-hosted [Starter](/deploy/introduction): it runs AgentOS with PostgreSQL (pgvector) on Docker Compose and takes the same stack to production on any host you control, from a VPS or home server to this laptop. The deploy layer is a single Compose override; there is no provider CLI and nothing to provision.

It ships with three agents:

| Agent | Role |
|-------|------|
| **Agent Builder** | Creates agents, teams, and workflows through the AgentOS Studio. |
| **Platform Manager** | Read-only ops: eval history, deployment checks, schedules, codebase questions. |
| **WebSearch** | Searches the web with Parallel. The simplest agent to copy. |

## Set up with a coding agent

The fastest way to a running platform. Copy the prompt into Claude Code, Cursor, or Codex; the agent clones the repo, configures credentials, starts Docker, proves the MCP endpoint end to end, and connects the frontends you pick.

```text expandable
Help me set up an AgentOS on this machine. Work step by step and verify each step before moving to the next. When a step needs me (an API key, a Docker install, a sign-in), stop, tell me exactly what to do, and wait for my input. Never read or print secrets.

1. Clone https://github.com/agno-agi/agentos-docker.git and cd in. Then read AGENTS.md end to end — it is the source of truth for how this platform works and answers most questions you'll hit along the way.
2. Run `cp example.env .env`, open .env in my favorite editor, and ask me to set the OPENAI_API_KEY.
3. Confirm docker is installed, running and `docker info` succeeds. If Docker is missing, ask me to install Docker Desktop and wait until it's running.
4. Start the platform with `docker compose up -d --build`, then poll http://localhost:8000/docs until it returns 200 (first build takes a few minutes). If it never comes up, read `docker compose logs agentos-api` and fix what you find.
5. Prove it end to end with ./scripts/mcp_check.sh — it should print "MCP OK" and a real agent answer. Show me that answer: it's my platform talking.
6. Ask me which frontends I want connected, then set up the ones I pick:
   - Coding agents (including you): run `uvx agno connect` — it registers http://localhost:8000/mcp in Claude Code, Claude Desktop, Codex, and Cursor, and verifies with a real handshake. Use the default user scope, not --project (that would write a token into the repo).
   - The AgentOS web UI: walk me through os.agno.com → Connect OS → http://localhost:8000, named "Local AgentOS".
   - Claude and ChatGPT apps (web or desktop): their sessions run in the cloud and can't reach localhost, so work with me on getting a public URL first, following the "Run in production" section of README.md — a tunnel (cloudflared, ngrok, or `tailscale funnel`) in front of this machine, then production mode with a JWT key I mint at os.agno.com. Then I add https://<public-url>/mcp as a custom connector in the chat app's connector settings.
7. Finish with a short summary of what's running and where, plus a few first prompts to try — start with asking Agent Builder to "Build an agent that tracks AI news and writes a daily brief".
```

Prefer to drive yourself? Follow the manual steps below.

## Manual setup

> **Prerequisites:** [Docker](https://www.docker.com/get-started/) installed and running. An [OpenAI API key](https://platform.openai.com).

<Steps>
  <Step title="Clone and configure">
```bash
git clone https://github.com/agno-agi/agentos-docker.git agentos
cd agentos

cp example.env .env
```

Edit `.env` and set `OPENAI_API_KEY`.
  </Step>

  <Step title="Start the platform">
```bash
docker compose up -d --build
```

The first build takes a few minutes. Confirm the API at [localhost:8000/docs](http://localhost:8000/docs).
  </Step>

  <Step title="Verify end to end">
```bash
./scripts/mcp_check.sh
```

Prints `MCP OK` with the tool count and a real agent answer through the MCP endpoint.
  </Step>

  <Step title="Connect the AgentOS UI">
1. Open [os.agno.com](https://os.agno.com) and sign in.
2. Click **Connect OS**, enter `http://localhost:8000`, and name it **Local AgentOS**.
  </Step>

  <Step title="Build your first agent">
Chat with **Agent Builder**: "Build an agent that tracks AI news and writes a daily brief". Then ask **Platform Manager**: "How healthy is the platform?"
  </Step>
</Steps>

<Check>Your AgentOS is running locally.</Check>

## Connect your frontends

| Frontend | How |
|----------|-----|
| Coding agents (Claude Code, Codex, Cursor) | `uvx agno connect` registers `http://localhost:8000/mcp` and verifies with a real handshake. |
| AgentOS UI | [os.agno.com](https://os.agno.com) → **Connect OS** → `http://localhost:8000`. |
| Claude and ChatGPT apps | Hosted sessions can't reach localhost. Run in production first, then add `https://<your-public-url>/mcp` as a custom connector. |
| Slack | Set `SLACK_BOT_TOKEN` and `SLACK_SIGNING_SECRET`. See [Slack setup](/deploy/interfaces/slack/overview). |
| Your product | REST API with 80+ endpoints. Browse them at `/docs`. |

## Run in production

> **Prerequisites:** a host with Docker Compose v2.24.4 or newer (the prod override uses the `!reset` and `!override` merge tags), and a way for the internet to reach port 8000 on it: a domain with a reverse proxy, or a tunnel.

<Steps>
  <Step title="Get a public URL">
The platform needs a public HTTPS URL for two things: hosted chat apps reaching `/mcp`, and `AGENTOS_URL`, the address the platform advertises as its own. Any of these work:

```bash
# Cloudflare Tunnel: free; quick tunnels get a random URL, named tunnels a stable one
cloudflared tunnel --url http://localhost:8000

# ngrok: reserved domains on paid plans
ngrok http 8000

# Tailscale Funnel: stable HTTPS URL on your tailnet's domain
tailscale funnel 8000
```

For a first run, an ephemeral cloudflared or ngrok URL is fine. For a real deployment, use something stable: a named Cloudflare tunnel, a reserved ngrok domain, or your own domain in front of a reverse proxy (Caddy, nginx) that forwards to port 8000.
  </Step>

  <Step title="Set production values">
Production values live in `.env` on the host, the same file Compose already reads:

```bash
OPENAI_API_KEY=sk-...
AGENTOS_URL=https://<your-public-url>
DB_PASS=<generate a strong one>
```

Left unset, `AGENTOS_URL` makes the daily deployment check flag the platform as misconfigured, and chat-app connectors have nothing to point at. `DB_PASS` replaces the dev default (`ai`).

<Note>On a host that already ran the dev Compose, changing `DB_PASS` in `.env` does not update the database. See [troubleshooting](/deploy/templates/docker/reference#troubleshooting).</Note>
  </Step>

  <Step title="Mint your JWT key">
Token-Based Authorization is on by default; without a `JWT_VERIFICATION_KEY`, the platform refuses to serve production traffic.

1. Open [os.agno.com](https://os.agno.com), click **Connect OS** → **Live**, enter your public URL, and name it **Live AgentOS**.
2. Go to **Settings** → **OS & Security** and turn on **Token-Based Authorization (JWT)**.
3. Copy the public key and paste it into `.env` with quotes, so Docker Compose reads the multi-line PEM as one value:

```bash
JWT_VERIFICATION_KEY="-----BEGIN PUBLIC KEY-----
MIIBIjANBgkq...
-----END PUBLIC KEY-----"
```

<Note>Live AgentOS connections are a paid feature. Use code `PLATFORM30` for one month off.</Note>
  </Step>

  <Step title="Start in production mode">
```bash
docker compose -f compose.yaml -f compose.prod.yaml up -d --build
```

The override switches `RUNTIME_ENV` to `prd` (JWT auth on), drops the dev bind mount and hot reload so the container runs the code baked into the image, passes your `AGENTOS_URL` through, and rebinds Postgres to loopback so only this host can reach it. Both services carry `restart: unless-stopped`, so the platform survives reboots as long as Docker starts on boot.
  </Step>

  <Step title="Confirm it's live">
```bash
curl https://<your-public-url>/health   # 200: /health and /docs stay public
curl https://<your-public-url>/agents   # 401: everything else wants a token
```

Logs, when something looks off:

```bash
docker compose -f compose.yaml -f compose.prod.yaml logs -f agentos-api
```
  </Step>
</Steps>

<Check>Your AgentOS is live on your own host.</Check>

### Production auth

Token-Based Authorization gives you three things:

1. **No public access.** The server rejects requests without a valid token.
2. **Per-request identity.** Middleware parses `user_id`, `session_id`, and custom claims from the token.
3. **Granular permissions.** User tokens run agents and read their own sessions. Admin tokens read everything.

The same middleware protects `/mcp`: clients authenticate with a bearer token, either a JWT minted at os.agno.com or a service-account PAT (`uvx agno connect` mints one automatically). To opt out inside a private network behind another auth layer, set `authorization=False` in `app/main.py` and restart.

### Redeploy after code changes

```bash
git pull   # or edit in place
docker compose -f compose.yaml -f compose.prod.yaml up -d --build
```

Env changes are the same command without `--build`; Compose recreates the container with the new `.env` values.

## Environment variables

The variables you touch during setup. Full table in the [reference](/deploy/templates/docker/reference#environment-variables).

| Variable | Required | Description |
|----------|----------|-------------|
| `OPENAI_API_KEY` | Yes | Models and embeddings. |
| `JWT_VERIFICATION_KEY` | Production | Public key from os.agno.com. The platform refuses production traffic without it. |
| `AGENTOS_URL` | No | Scheduler base URL. Set it to your public URL in production. |
| `DB_PASS` | No | Postgres password. Replace the dev default (`ai`) in production. |
| `SLACK_BOT_TOKEN` / `SLACK_SIGNING_SECRET` | No | Set both to enable the Slack interface. |
| `PARALLEL_API_KEY` | No | WebSearch uses the Parallel SDK when set, keyless MCP otherwise. |

## Next steps

<CardGroup cols={3}>
  <Card title="Build with coding agents" icon="wand-magic-sparkles" href="/deploy/templates/improve-agents">
    Skills for the create → improve → evaluate → maintain lifecycle.
  </Card>
  <Card title="Docker reference" icon="book" href="/deploy/templates/docker/reference">
    Commands, environment variables, troubleshooting.
  </Card>
  <Card title="Interfaces" icon="comments" href="/deploy/interfaces/overview">
    Connect WhatsApp, Telegram, Discord, or a custom UI.
  </Card>
</CardGroup>

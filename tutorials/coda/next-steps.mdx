---
title: Next Steps
description: "Add repos, schedule digests, write code in worktrees."
---

A teammate that only responds is a bot. Coda becomes useful when it shows up on its own. The Coda repo has the canonical setup for each piece below.

## Add more repos

Edit [`repos.yaml`](https://github.com/agno-agi/coda/blob/main/repos.yaml) to add the repos you want Coda to know about:

```yaml
repos:
  - url: https://github.com/your-org/repo-a
    branch: main
  - url: https://github.com/your-org/repo-b
    branch: develop
```

Two requirements that bite people:

1. The repo must be in your GitHub token's "Only select repositories" list. Without that, Coda can't clone it.
2. The token needs **Contents R/W**, **Pull requests R/W**, **Issues R/W**, **Metadata R**. Anything missing fails silently.

Full GitHub token setup (with the Fine-grained PAT walkthrough): [GITHUB_ACCESS.md](https://github.com/agno-agi/coda/blob/main/docs/GITHUB_ACCESS.md).

After editing `repos.yaml`, restart to clone the new repos:

```bash
docker compose up -d
```

## Turn on scheduled tasks

Coda ships three scheduled tasks. Two are gated on env vars:

| Task | Schedule | What it does | How to enable |
|------|----------|--------------|---------------|
| **Repo Sync** | Every 5 minutes | Pulls latest changes from every configured repo | On by default |
| **Daily Digest** | Each morning | Posts what merged yesterday, what PRs are waiting, what issues opened, what's gone stale | Set `DIGEST_CHANNEL` |
| **Issue Triage** | Daily | Reviews open issues against the actual codebase, labels them, posts a summary | Set `TRIAGE_CHANNEL` |

Channel IDs come from Slack: right-click the channel → **View details** → copy the ID at the bottom (looks like `C0123456789`).

```bash
# .env
DIGEST_CHANNEL=C0123456789
TRIAGE_CHANNEL=C0987654321
```

Restart, and the schedules pick up. You can also trigger triage on-demand with `POST /triage-issues`.

<Note>
  🎥 **Add video:** Daily digest landing in a Slack channel showing yesterday's merges, open PRs, new issues.
</Note>

## How the code-writing flow stays out of main

When you ask Coda to write code, the Coder agent works in a git worktree, not on your main branch:

```
@Coda add rate limiting to /api/v1/users using the same pattern as /orders
@Coda fix the NoResultFound bug in payment_service
@Coda write integration tests for the export endpoint
```

The flow:

1. Coder creates a `coda/<branch-name>` worktree off main.
2. Reads the relevant code, plans the change, writes it.
3. Runs whatever validation it has access to.
4. Pushes the branch and opens a PR.
5. A human reviews and merges. **Coda never merges.**

Your main branch is never touched. Worktrees clean up on restart.

## Build your own scheduled tasks

Coda's tasks live in [`tasks/`](https://github.com/agno-agi/coda/tree/main/tasks). Each is a function registered with the AgentOS scheduler in [`app/main.py`](https://github.com/agno-agi/coda/blob/main/app/main.py). The same pattern works for anything you want Coda to do on a cadence:

- **Auto PR review** when new PRs are opened
- **Stale branch alerts** for branches with no commits in N days
- **Convention drift detection** comparing recent merges against your style guide
- **Weekly summaries** beyond the daily digest

The [Scheduling](/runtime/scheduling) page has the API reference for the runtime side.

## Coda's learning loop

Coda gets sharper the more you use it. First time you ask for a new endpoint: generic patterns. After a few weeks of use on your codebase: it follows your service layer conventions, your error handling, your naming style, your logging.

Powered by [Agno's Learning Machine](/learning/overview). Patterns Coda discovers get stored as learnings and surfaced on future runs. No prompt engineering, no manual feedback collection.

## Going deeper

| To learn | See |
|----------|-----|
| The team architecture | [`coda/team.py`](https://github.com/agno-agi/coda/blob/main/coda/team.py) and [`coda/agents/`](https://github.com/agno-agi/coda/tree/main/coda/agents) |
| Run evals | `python -m evals.run --category security` |
| Use Parallel for premium web research | Set `PARALLEL_API_KEY` from [parallel.ai](https://parallel.ai) |
| Run local + production in parallel (separate Slack apps) | [Coda README](https://github.com/agno-agi/coda#running-local--production) |
| Comparable templates | [Scout](/tutorials/scout/overview), [Dash](/tutorials/dash/overview) |
| Building a fully custom AgentOS app | [Build a Product](/runtime/build-a-product) |
